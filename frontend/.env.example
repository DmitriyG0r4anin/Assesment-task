VITE_GRAPHQL_URL=/graphql
VITE_NOTIFICATIONS_URL=/notifications/motionHub

# Docker compose uses relative URLs (/graphql, /notifications/motionHub)
# proxied by nginx in the frontend container — see frontend/nginx.conf